{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite"
        ,"icons:generate": "node tools/convert-icons.js",
        "build:assets": "npm run icons:generate && vite build"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^10.0.3",
        "laravel-vite-plugin": "^3.1",
        "sharp": "^0.35.2",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    },
    "overrides": {
        "shell-quote": "^1.8.4"
    }
}
