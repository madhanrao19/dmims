{
    "$schema": "https://www.schemastore.org/package.json",
    "name": "dmims",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "icons:generate": "node tools/convert-icons.js",
        "build:assets": "vite build && npm run icons:generate",
        "qa": "playwright test",
        "qa:headed": "playwright test --headed",
        "qa:ui": "playwright test --ui",
        "qa:report": "playwright show-report"
    },
    "devDependencies": {
        "@playwright/test": "^1.61.1",
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^10.0.3",
        "laravel-vite-plugin": "^3.1",
        "sharp": "^0.35.0",
        "tailwindcss": "^4.0.0",
        "vite": "^8.1.3"
    },
    "overrides": {
        "shell-quote": "^1.8.4"
    }
}
